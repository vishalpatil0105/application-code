name: CI/CD Pipeline

on:
  # push:
  #   branches:
  #     - main
  # pull_request:
  #   branches:
  #     - main
  workflow_dispatch:

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
      # Step 1: Checkout the repository
      - name: Checkout repository
        uses: actions/checkout@v4

      # Step 2: Set up Python for running tests
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.9"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run tests
        run: |
          python -m unittest discover tests

      # Step 3: Log in to Docker Hub
      # - name: Log in to Docker Hub
      #   uses: docker/login-action@v2
      #   with:
      #     username: ${{ secrets.DOCKER_USERNAME }}
      #     password: ${{ secrets.DOCKER_PASSWORD }}

      # # Step 4: Build the Docker image
      # - name: Build Docker image
      #   run: |
      #     docker build -t ${{ secrets.DOCKER_USERNAME }}/my-flask-app:latest .

      # # Step 5: Push the Docker image to Docker Hub
      # - name: Push Docker image to Docker Hub
      #   run: |
      #     docker push ${{ secrets.DOCKER_USERNAME }}/my-flask-app:latest
      
      - name: Store current run ID
        run: echo "RUN_ID=${{ github.run_id }}" >> $GITHUB_ENV

  update-the-image-tag:
    runs-on: ubuntu-latest
    needs: build-and-push
    steps:
      # - name: Checkout repository-helmcharts
      #   uses: actions/checkout@v4
      #   with:
      #     path: helmcharts

      # - name: run-python-script
      #   run: /
      #     python ./helmcharts/script.py {{  }}
      
      - name: Echo the run ID from the previous job
        run: |
          echo "The current run ID is $RUN_ID"
